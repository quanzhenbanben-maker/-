# .env.example（GitHubに上げる・値は書かない）
OPENAI_API_KEY=
GOOGLE_MAPS_API_KEY=
HOTPEPPER_API_KEY=


#なぜ .env.example を作るのか
#.env 本体はGitに入れない、でも「どんなキーが必要か」は全員が知る必要がある。
#そのために「キー名だけ書いたサンプルファイル」として .env.example を作る。